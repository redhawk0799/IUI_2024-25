

# power for TeslaSuit
power = {
    "soft": 40,
    "medium": 80,
    "strong": 150
}

duration_of_touch = 500     # in ms, has to match with the interval in which the mean of the recording gets calculated!

# threshold values for touch
thresholds = {
    "soft": [40, 100],
    "medium": [100, 200],
    "strong": [200]
}